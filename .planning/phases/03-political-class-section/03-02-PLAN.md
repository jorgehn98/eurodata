---
phase: 03-political-class-section
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/types/politics.ts
  - src/hooks/usePoliticalMetric.ts
  - src/hooks/usePoliticalPensions.ts
  - src/hooks/useRevolvingDoorCases.ts
  - messages/es.json
  - messages/en.json
autonomous: true
requirements:
  - POLI-01
  - POLI-02
  - POLI-03
  - POLI-04
  - POLI-05
  - POLI-06
  - POLI-07
  - POLI-08

must_haves:
  truths:
    - "TypeScript types PoliticalDataPoint, PoliticalPension, RevolvingDoorCase are exported and fully typed"
    - "POLITICAL_METRICS const array defines all 7 metric strings used in political_data table"
    - "usePoliticalMetric hook fetches a single metric from political_data and returns PoliticalDataPoint[]"
    - "usePoliticalPensions hook fetches all rows from political_pensions sorted by exit_year DESC"
    - "useRevolvingDoorCases hook fetches all rows from revolving_door_cases sorted by year DESC"
    - "messages/es.json and messages/en.json contain a Politics namespace with all UI strings for the section"
  artifacts:
    - path: "src/types/politics.ts"
      provides: "PoliticalDataPoint, PoliticalPension, RevolvingDoorCase types; POLITICAL_METRICS const; PoliticalMetric union"
      exports: ["PoliticalDataPoint", "PoliticalPension", "RevolvingDoorCase", "POLITICAL_METRICS", "PoliticalMetric"]
    - path: "src/hooks/usePoliticalMetric.ts"
      provides: "TanStack Query hook for political_data table"
      exports: ["usePoliticalMetric"]
    - path: "src/hooks/usePoliticalPensions.ts"
      provides: "TanStack Query hook for political_pensions table"
      exports: ["usePoliticalPensions"]
    - path: "src/hooks/useRevolvingDoorCases.ts"
      provides: "TanStack Query hook for revolving_door_cases table"
      exports: ["useRevolvingDoorCases"]
  key_links:
    - from: "src/hooks/usePoliticalMetric.ts"
      to: "supabase political_data table"
      via: "createClient().from('political_data').select().eq('country_id', SPAIN_COUNTRY_ID).eq('metric', metric)"
      pattern: "from.*political_data.*eq.*country_id"
    - from: "src/hooks/usePoliticalPensions.ts"
      to: "supabase political_pensions table"
      via: "createClient().from('political_pensions').select().order('exit_year', { ascending: false })"
      pattern: "from.*political_pensions.*order.*exit_year"
    - from: "src/hooks/useRevolvingDoorCases.ts"
      to: "supabase revolving_door_cases table"
      via: "createClient().from('revolving_door_cases').select().order('year', { ascending: false })"
      pattern: "from.*revolving_door_cases.*order.*year"
---

<objective>
Create the TypeScript types, TanStack Query hooks, and i18n translation strings for the Political Class Section. This plan defines the data access contracts that Plan 03-03 UI components will implement against.

Purpose: The UI plan (03-03) depends on stable types and hooks. Defining them first prevents the executor from guessing at interfaces. The i18n strings establish all chart titles, labels, and section copy in both Spanish and English.
Output: 4 TypeScript files (types + 3 hooks), both message files updated with Politics namespace.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-political-class-section/03-CONTEXT.md
@.planning/phases/03-political-class-section/03-RESEARCH.md

# Exact patterns to follow — read these before implementing
@src/types/economy.ts
@src/hooks/useEconomyMetric.ts
@src/lib/constants.ts
@messages/es.json
@messages/en.json
</context>

<interfaces>
<!-- From src/types/economy.ts (pattern to follow exactly):
export type EconomyDataPoint = {
  year: number;
  value: number | null;
  source: string;
  source_url: string;
};
export const ECONOMY_METRICS = [...] as const;
export type EconomyMetric = typeof ECONOMY_METRICS[number];
-->

<!-- From src/hooks/useEconomyMetric.ts (pattern to follow exactly):
'use client';
import { useQuery } from '@tanstack/react-query';
import { createClient } from '@/lib/supabase/client';
import { SPAIN_COUNTRY_ID } from '@/lib/constants';
import type { EconomyDataPoint, EconomyMetric } from '@/types/economy';

export function useEconomyMetric(metric: EconomyMetric) {
  const supabase = createClient();  // inside hook body, NOT module scope
  return useQuery<EconomyDataPoint[]>({
    queryKey: ['economy', metric],
    queryFn: async () => {
      const { data, error } = await supabase
        .from('economic_indicators')
        .select('year, value, source, source_url')
        .eq('country_id', SPAIN_COUNTRY_ID)
        .eq('metric', metric)
        .order('year', { ascending: true });
      if (error) throw new Error(error.message);
      return (data ?? []).map((row) => ({
        year: row.year as number,
        value: row.value !== null ? Number(row.value) : null,
        source: row.source as string,
        source_url: row.source_url as string,
      }));
    },
    staleTime: 5 * 60 * 1000,
  });
}
-->

<!-- SPAIN_COUNTRY_ID = 26 (from src/lib/constants.ts) -->

<!-- DB tables for this plan:
  political_data: (country_id, metric, year, value, unit, source, source_url)
    metric values: president_salary_nominal, president_salary_real,
                   minister_salary_nominal, minister_salary_real,
                   mp_salary_nominal, mp_salary_real, advisor_count
  political_pensions: (id, name, role, pension_annual_eur, years_in_office, exit_year, source, source_url)
  revolving_door_cases: (id, person_name, political_role, entity_moved_to, year, source_url)
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create politics types and hooks</name>
  <files>
    src/types/politics.ts
    src/hooks/usePoliticalMetric.ts
    src/hooks/usePoliticalPensions.ts
    src/hooks/useRevolvingDoorCases.ts
  </files>
  <action>
Create four TypeScript files following the exact patterns from `src/types/economy.ts` and `src/hooks/useEconomyMetric.ts`.

**`src/types/politics.ts`:**
```typescript
// src/types/politics.ts
// Data shapes for the Political Class Section.
// Matches DB tables: political_data, political_pensions, revolving_door_cases.

// Returned by usePoliticalMetric — matches political_data columns
export type PoliticalDataPoint = {
  year: number;
  value: number | null;  // null = gap year; chart shows break, not zero
  source: string;
  source_url: string;
};

// All metric strings seeded in political_data for Spain.
// Salary metrics stored in two variants: nominal (current EUR) and real (2015-adjusted EUR).
// Compile-time error if metric string is mistyped.
export const POLITICAL_METRICS = [
  'president_salary_nominal',
  'president_salary_real',
  'minister_salary_nominal',
  'minister_salary_real',
  'mp_salary_nominal',
  'mp_salary_real',
  'advisor_count',
] as const;

export type PoliticalMetric = typeof POLITICAL_METRICS[number];

// Returned by usePoliticalPensions — matches political_pensions columns
export type PoliticalPension = {
  id: number;
  name: string;
  role: string;
  pension_annual_eur: number;
  years_in_office: number;
  exit_year: number;
  source: string;
  source_url: string;
};

// Returned by useRevolvingDoorCases — matches revolving_door_cases columns
export type RevolvingDoorCase = {
  id: number;
  person_name: string;
  political_role: string;
  entity_moved_to: string;
  year: number;
  source_url: string;
};
```

**`src/hooks/usePoliticalMetric.ts`:**
Mirror `useEconomyMetric.ts` exactly, substituting:
- Table: `'political_data'` instead of `'economic_indicators'`
- Type: `PoliticalDataPoint` and `PoliticalMetric`
- queryKey: `['politics', metric]`
- `createClient()` inside hook body (never at module scope)
- Same `staleTime: 5 * 60 * 1000`
- Same `Number(row.value)` cast pattern

**`src/hooks/usePoliticalPensions.ts`:**
```typescript
// src/hooks/usePoliticalPensions.ts
'use client';
import { useQuery } from '@tanstack/react-query';
import { createClient } from '@/lib/supabase/client';
import type { PoliticalPension } from '@/types/politics';

export function usePoliticalPensions() {
  const supabase = createClient();

  return useQuery<PoliticalPension[]>({
    queryKey: ['politics', 'pensions'],
    queryFn: async () => {
      const { data, error } = await supabase
        .from('political_pensions')
        .select('id, name, role, pension_annual_eur, years_in_office, exit_year, source, source_url')
        .order('exit_year', { ascending: false });

      if (error) throw new Error(error.message);

      return (data ?? []).map((row) => ({
        id: row.id as number,
        name: row.name as string,
        role: row.role as string,
        pension_annual_eur: Number(row.pension_annual_eur),
        years_in_office: row.years_in_office as number,
        exit_year: row.exit_year as number,
        source: row.source as string,
        source_url: row.source_url as string,
      }));
    },
    staleTime: 5 * 60 * 1000,
  });
}
```

**`src/hooks/useRevolvingDoorCases.ts`:**
Mirror `usePoliticalPensions.ts` pattern, substituting:
- Table: `'revolving_door_cases'`
- Select: `'id, person_name, political_role, entity_moved_to, year, source_url'`
- Order: `year` descending
- Return type: `RevolvingDoorCase`
- queryKey: `['politics', 'revolving-door']`
- Map all string columns explicitly with `as string` cast

No `country_id` filter needed for pensions and revolving door tables — they have no country_id column (Spain-specific data only in v1).
  </action>
  <verify>
```bash
cd /c/Users/jorge/Desktop/EuroScope && npx tsc --noEmit 2>&1
```
TypeScript compilation exits 0 with no errors.

Also verify file exports:
```bash
grep -n "export" src/types/politics.ts src/hooks/usePoliticalMetric.ts src/hooks/usePoliticalPensions.ts src/hooks/useRevolvingDoorCases.ts
```
Each file shows the expected exports.
  </verify>
  <done>4 files created. `npx tsc --noEmit` passes. POLITICAL_METRICS contains all 7 metric strings. PoliticalPension and RevolvingDoorCase types match DB column names exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Politics namespace to i18n translation files</name>
  <files>
    messages/es.json
    messages/en.json
  </files>
  <action>
Add a `Politics` namespace to both translation files. Follow the exact structure of the existing `Economy` namespace. All chart titles MUST comply with POLI-07 (neutral — describe the data axis, never a conclusion).

Read the current content of both files before editing. Add the `Politics` key at the same level as `Economy`.

**Spanish (`messages/es.json`) — add under top-level keys:**
```json
"Politics": {
  "title": "Clase Política",
  "intro": "Retribuciones, asesores, pensiones y puertas giratorias de los cargos públicos en España, con datos oficiales del BOE y Portal de Transparencia.",
  "noData": "Sin datos para este año",
  "dataUnavailable": "Datos no disponibles",
  "sourcePrefix": "Fuente",
  "gapsNote": "Los espacios en el gráfico indican años sin datos disponibles.",
  "dataThrough": "Datos hasta",
  "anchors": {
    "salaryComparison": "Salarios",
    "salaryRatio": "Ratios",
    "advisors": "Asesores",
    "pensions": "Pensiones",
    "revolvingDoor": "Puerta giratoria"
  },
  "toggle": {
    "real": "Real (ajustado inflación)",
    "nominal": "Nominal (EUR corrientes)"
  },
  "charts": {
    "salaryComparison": "Salario bruto anual (EUR)",
    "salaryRatio": "Ratio salario político / salario mediano",
    "advisorCount": "Cargos de libre designación por legislatura"
  },
  "series": {
    "president": "Presidente del Gobierno",
    "ministers": "Ministros (promedio)",
    "mps": "Diputados (promedio)",
    "median": "Salario mediano ciudadano"
  },
  "pensions": {
    "sectionTitle": "Pensiones vitalicias (EUR/año)",
    "columns": {
      "name": "Nombre",
      "role": "Cargo",
      "pension": "Pensión anual (EUR)",
      "yearsInOffice": "Años en el cargo",
      "source": "Fuente"
    },
    "sourceLabel": "BOE"
  },
  "revolvingDoor": {
    "sectionTitle": "Casos documentados de puerta giratoria",
    "fields": {
      "role": "Cargo político",
      "movedTo": "Destino",
      "year": "Año",
      "source": "Fuente"
    },
    "sourceLabel": "Ver fuente"
  }
}
```

**English (`messages/en.json`) — add under top-level keys:**
```json
"Politics": {
  "title": "Political Class",
  "intro": "Official salaries, advisors, pensions and revolving door cases for public office holders in Spain, sourced from BOE and the Transparency Portal.",
  "noData": "No data for this year",
  "dataUnavailable": "Data unavailable",
  "sourcePrefix": "Source",
  "gapsNote": "Gaps in the chart indicate years with no available data.",
  "dataThrough": "Data through",
  "anchors": {
    "salaryComparison": "Salaries",
    "salaryRatio": "Ratios",
    "advisors": "Advisors",
    "pensions": "Pensions",
    "revolvingDoor": "Revolving door"
  },
  "toggle": {
    "real": "Real (inflation-adjusted)",
    "nominal": "Nominal (current EUR)"
  },
  "charts": {
    "salaryComparison": "Annual gross salary (EUR)",
    "salaryRatio": "Politician salary / median salary ratio",
    "advisorCount": "Free-appointment positions by legislature"
  },
  "series": {
    "president": "President",
    "ministers": "Ministers (average)",
    "mps": "MPs (average)",
    "median": "Citizen median salary"
  },
  "pensions": {
    "sectionTitle": "Lifetime pensions (EUR/year)",
    "columns": {
      "name": "Name",
      "role": "Role",
      "pension": "Annual pension (EUR)",
      "yearsInOffice": "Years in office",
      "source": "Source"
    },
    "sourceLabel": "BOE"
  },
  "revolvingDoor": {
    "sectionTitle": "Documented revolving door cases",
    "fields": {
      "role": "Political role",
      "movedTo": "Moved to",
      "year": "Year",
      "source": "Source"
    },
    "sourceLabel": "View source"
  }
}
```

Verify chart title strings pass POLI-07: "Salario bruto anual (EUR)" — describes the Y-axis unit. "Ratio salario político / salario mediano" — describes the mathematical relationship. "Cargos de libre designación por legislatura" — describes the Y-axis (counts). None state a conclusion or judgment. All pass.
  </action>
  <verify>
```bash
cd /c/Users/jorge/Desktop/EuroScope && npx tsc --noEmit 2>&1 && node -e "const es = require('./messages/es.json'); const en = require('./messages/en.json'); console.log('ES Politics keys:', Object.keys(es.Politics).join(', ')); console.log('EN Politics keys:', Object.keys(en.Politics).join(', '));"
```
Both namespaces present. TypeScript compilation still passes.
  </verify>
  <done>messages/es.json and messages/en.json both contain a `Politics` namespace with all sub-keys (title, intro, anchors, toggle, charts, series, pensions, revolvingDoor). Chart title strings are neutral — axis descriptions only. `npx tsc --noEmit` still passes.</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/jorge/Desktop/EuroScope && npx tsc --noEmit 2>&1
```
Zero TypeScript errors. All 4 new files export their types/functions correctly.
</verification>

<success_criteria>
- src/types/politics.ts exports PoliticalDataPoint, PoliticalPension, RevolvingDoorCase, POLITICAL_METRICS (7 items), PoliticalMetric
- src/hooks/usePoliticalMetric.ts: createClient() inside hook, queryKey ['politics', metric], staleTime 5 min, returns PoliticalDataPoint[]
- src/hooks/usePoliticalPensions.ts: fetches political_pensions ordered by exit_year DESC, returns PoliticalPension[]
- src/hooks/useRevolvingDoorCases.ts: fetches revolving_door_cases ordered by year DESC, returns RevolvingDoorCase[]
- Both message files have Politics namespace with all required keys
- npx tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-political-class-section/03-02-SUMMARY.md`
</output>
